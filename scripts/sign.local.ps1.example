# 代码签名配置 —— 复制此文件为 sign.local.ps1 后生效 (该文件已 gitignore)
#
# 【不创建 sign.local.ps1 时, dev.ps1 的行为与从前逐字不变】——
# 构建、部署、打包全部照旧, 只是产物不带签名。
#
# ⚠️ 就算配好了, 签名也【默认不发生】: 必须在命令里显式写 sign 才签。
#    签名次数按月计费且有限, 一次全构建就是 5 个 PE、加打包 7 次 —— 若做成
#    "配了就自动签", 日常 d1 几天就能把一个月的额度烧光。
#
# ⚠️ 本仓的脚本与文档【刻意不提任何签名服务商的名字】。签名平台是可替换的外部依赖,
#    把它焊进公开仓库既没必要 (换一家就要全仓改名), 也不合适 (对外暴露供应链细节)。
#    服务商相关的一切都落在这个文件里, 而这个文件不入库。
#
#
# ══════════════════════════════════════════════════════════════════════════
#  一、一次性准备
# ══════════════════════════════════════════════════════════════════════════
#
#  签名走的是【云签名】: 私钥在服务商的 HSM 里, 不可导出, 也不涉及任何物理硬件
#  (没有 USB token, 没有实体智能卡)。本机要能签名, 只需满足一件事:
#
#      证书出现在 Cert:\CurrentUser\My, 且私钥可用。
#
#  1. 装上服务商的本地客户端, 按其说明登录、建立签名会话。
#     登录成功后证书会自动进入 Windows 证书存储 —— 这就是 signtool 要的全部条件。
#
#     ⚠️ 会话【有时限】(本项目所用服务商是 2 小时), 过期后要重新做一次二次验证。
#        这条限制决定了签名做不成无人值守的 CI 步骤, 详见 docs\design\code-signing.md。
#
#  2. 取证书指纹 (会话开启状态下跑):
#       Get-ChildItem Cert:\CurrentUser\My | Where-Object { $_.HasPrivateKey } |
#           Format-List Subject, Thumbprint, NotAfter
#     把你那张代码签名证书的 Thumbprint 填到下面。
#
#  3. 验证: `.\scripts\sign.ps1 -Status` 应报「会话: 可用」并列出证书主体与有效期。
#
#
# ══════════════════════════════════════════════════════════════════════════
#  二、必填项
# ══════════════════════════════════════════════════════════════════════════

# 证书指纹 (40 位十六进制, 大小写与空格都无所谓, 脚本会归一化)。
# 不定义此变量 = 签名整体关闭。
#
# 这【不是】秘密: thumbprint 就是证书的 SHA-1, 每个签过名的文件里都带着它。
# 知道指纹只能指定「用哪张证书」, 不能拿它签任何东西。放心写进本文件、放心写进 CI 变量。
# 真正的秘密是签名服务的账号口令与二次验证的 TOTP 种子 —— 那两样【绝不】出现在
# 任何文件里, 本脚本也不接受它们。
$WIND_SIGN_THUMBPRINT = "0000000000000000000000000000000000000000"


# ══════════════════════════════════════════════════════════════════════════
#  三、可选项
# ══════════════════════════════════════════════════════════════════════════

# 会话不可用时打印的排障提示。默认是一句中性文案; 填上你那家服务商的具体步骤
# (客户端叫什么、在哪启动、会话怎么开), 排障时能省一次翻文档。
# $WIND_SIGN_SESSION_HINT = "打开 <客户端名称> → 用 <二次验证方式> 登录"

# RFC3161 时间戳服务器。默认是一个通用公共服务。
#
# ⚠️ 时间戳【不可省略】。没有它, 签名会在证书到期当天集体失效 —— 连早已发出去、
#    用户机器上装着的那些安装包也会一起变成「未知发布者」。有时间戳则签名永久有效。
#
# 时间戳与签名证书的来源无关 (RFC3161 是通用协议), 默认值一般够用。你的 CA 若指定了
# 自家 TSA, 或默认这家不稳, 在此覆盖:
# $WIND_SIGN_TIMESTAMP_URL = "http://timestamp.sectigo.com"

# 单个文件的签名尝试上限 (含首次)。覆盖两类瞬时故障: 时间戳服务器抽风, 以及打包器刚
# 写完文件、句柄尚未释放时的占用 (实测会间歇性报 "file is being used by another process")。
# $WIND_SIGN_RETRY = 3

# 指定 signtool.exe。默认自动挑 Windows SDK 里版本号最大的 x64 版。
# $env:WIND_SIGNTOOL = "C:\Program Files (x86)\Windows Kits\10\bin\10.0.26100.0\x64\signtool.exe"


# ══════════════════════════════════════════════════════════════════════════
#  四、日常用法
# ══════════════════════════════════════════════════════════════════════════
#
#   .\scripts\sign.ps1 -Status         体检: 配置对不对、会话开没开、证书几时到期
#
#   sign 是【位置无关的开关】, 与构建/打包命令连用 (写不写决定这一次签不签):
#   .\scripts\dev.ps1 1                 全构建, 不签         (0 次)
#   .\scripts\dev.ps1 sign 1            全构建并签 5 个 PE   (5 次)
#   .\scripts\dev.ps1 sign 8            出安装包, 签 5 PE + 卸载器 + 外壳  (7 次)
#   .\scripts\dev.ps1 sign 9s           紧接着出便携包 —— 包内 PE 上一步已签过会跳过,
#                                        只是把已签的文件打进 zip  (0 次)
#   .\scripts\dev.ps1 verify-sign       验签: 确认 dist\ 下的产物都带有效签名
#
#   省配额的关键: 已签过的文件会按【签名者指纹】识别并跳过, 所以 `sign 8` 之后再
#   `sign 9s` 不会重复扣次数。反过来, 每次都重跑 `sign 8` 则每次都要签一次安装包。
#
#
# ══════════════════════════════════════════════════════════════════════════
#  五、已知陷阱
# ══════════════════════════════════════════════════════════════════════════
#
#  ⚠️ 会话过期时【不会】打断构建 —— 默认只警告并产出未签名包 (本地开发不该被签名挡住)。
#     正式发版请走 `.\scripts\dev.ps1 8 verify-sign`, 它会在最后硬校验一遍;
#     或直接 `.\scripts\sign.ps1 -Verify dist`。
#
#  ⚠️ 便携版 zip 本身【签不了】—— Authenticode 只认 PE。保护靠的是包内每个 exe/dll
#     都已签名, 加上 .sha256 sidecar。故 zip 必须在 PE 签完之后再压, 顺序不能倒。
#
#  ⚠️ 卸载器 uninstall.exe 【会签】, 但签名窗口只有一格: pack.ps1 -PrepOnly 之后、
#     -SkipPrep 之前。早了签的是随后会被 prep 改写的裸 stub, 晚了它已进压缩块。
#     (它一度签不了 —— 品牌清单原本在安装期追加到自身尾部, 而任何尾部追加都会让
#     Authenticode 失效; 解法是把 overlay 前移到打包期。见 docs\design\code-signing.md 4.2。)
#
#  ⚠️ 打包类实际消耗 7 次而非 6 次: 5 个 PE + uninstall.exe + Setup 外壳。
